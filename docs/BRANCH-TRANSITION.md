# Промпт: завершить Phase 1B и подготовить ветку следующего Goal

## Результат перехода (2026-09-11)

Выполнено. Phase 1B закоммичена как `e9b2f16cafe79421c8cf59f1d3fc028379888386` (`feat: add audited path-only resource classification`), `main` переведена fast-forward на этот коммит, создана ветка `codex/read-path-default-decisions`, в которой обновлены baseline handoff и checkpoint. Push не выполнялся, реализация следующего Goal не начиналась. Повторно исполнять инструкции ниже не нужно.

Работай в `/Users/2am./Projects/Personal/pi-warden`.

Выполни только переход Git и обновление документации ниже. Разрешены один локальный коммит принятой Phase 1B, fast-forward локальной `main`, создание `codex/read-path-default-decisions` и обновление baseline следующего handoff. Не начинай реализацию функции. Не выполняй push, не создавай PR, не удаляй ветки и локальные артефакты.

## 1. Проверь исходное состояние

Прочитай `AGENTS.md`, `STATE.md`, `docs/PHASE-1B-COMMIT.md`, `docs/PHASE-1B-AUDIT.md` и `IMPLEMENTATION_HANDOFF.md`. Проверь Git status, текущую ветку, HEAD, локальную `main`, index и список worktree.

Ожидается: текущая ветка `phase-1b-resource-classification`; HEAD и локальная `main` равны `548032665b57fdbaa4399ad2c0aaaa9ea001a2f6`; index пуст; новая ветка ещё не существует. Не считай локальную remote-tracking ссылку доказательством текущего состояния сервера. Сеть для этого задания не нужна.

Если переход частично уже выполнен, установи фактическое состояние и не повторяй коммит. При неизвестных новых изменениях, занятой в другом worktree `main`, расхождении веток или неоднозначности остановись с точным описанием. Ничего не перезаписывай ради совпадения с ожидаемым состоянием.

## 2. Сохрани и проверь разделение изменений

Phase 1B прошла независимый аудит и принята. Кандидат коммита содержит ровно 15 файлов, перечисленных в `docs/PHASE-1B-COMMIT.md`.

Патч: `/tmp/pi-warden-acceptance-20260911/phase-1b-accepted.patch`.

SHA-256 патча: `45e565e0faf7fe2d1ba80c04760a01810a1f08a0c5a50c9df2e9ef0e01eef1cf`.

Manifest: `/tmp/pi-warden-acceptance-20260911/manifest.json`.

Проверь патч, состав файлов, manifest и четыре source/test hash из отчёта аудита. Если временные артефакты пропали или hash не совпал, остановись: не восстанавливай разделение по догадке и не включай весь рабочий каталог в коммит.

Перед Git-изменениями сохрани вне репозитория проверяемые копии текущих документов, которые понадобятся после перехода: `STATE.md`, `ARCHITECTURE.md`, `IMPLEMENTATION_HANDOFF.md`, `docs/READ-PATH-DECISIONS.md`, `docs/PHASE-1B-COMMIT.md`, `docs/BRANCH-TRANSITION.md`, `docs/handoffs/20260911-resource-full-result-assertions.md`. Запиши их hashes и исходный status. Эти копии содержат более новый план, чем snapshot Phase 1B.

Не трогай содержимое `.qwen/`, `.opencode-permission-canary.txt` и изменение `.gitignore`. Не исполняй локальные конфигурации. Не используй `git add .`, `git add -A`, `git reset --hard`, `git clean`, force checkout, blanket restore или общий stash рабочего дерева.

## 3. Создай ровно один коммит Phase 1B

Подготовь index из проверенного патча относительно ожидаемого HEAD, сохранив рабочие файлы. Для пустого index подходят `git apply --cached --check <patch>` и затем `git apply --cached <patch>`; не применяй этот патч поверх уже заполненного рабочего дерева.

До коммита проверь весь staged diff, точный набор 15 файлов и hashes содержимого index против manifest. В нём не должно быть нового плана, этого промпта, active/archived handoff, `.gitignore` или локальных артефактов.

Выполни `npm run check` и `git diff --cached --check`. Проверка текущего рабочего дерева применима к коммиту только после подтверждения, что production, tests, package files и tsconfig совпадают с index/HEAD; различия допустимы только в сохранённых плановых документах и исключённых локальных артефактах. При ошибке остановись, не исправляй production в этом задании.

Создай коммит `feat: add audited path-only resource classification`. Запиши его полный SHA и проверь состав фактического коммита. Не обходи ошибки или проверки commit hooks отключением защит.

## 4. Обнови main и создай новую ветку

Перейди на локальную `main` и выполни только `git merge --ff-only phase-1b-resource-classification`. Если fast-forward невозможен, остановись без merge-коммита, rebase или принудительного изменения ссылок.

Учитывай, что после коммита в `STATE.md` и `ARCHITECTURE.md` останутся более новые незакоммиченные плановые изменения, способные помешать checkout. Не обходи отказ force-флагом. При необходимости временно приведи только эти два файла к принятому коммиту после проверки сохранённых полных копий; восстанови план из этих копий в новой ветке. Все остальные изменения сохрани. Не делай blanket restore.

Создай `codex/read-path-default-decisions` от обновлённой `main`. Проверь, что `main`, старая feature-ветка и HEAD новой ветки указывают на созданный Phase 1B commit. Старую ветку оставь.

## 5. Обнови handoff и checkpoint в новой ветке

Восстанови сохранённые плановые документы без потери текста, затем внеси только необходимые статусные изменения:

- В `IMPLEMENTATION_HANDOFF.md` замени baseline на полный SHA Phase 1B commit, укажи новую ветку и убери устаревшие утверждения о незакоммиченной Phase 1B и ожидающем переходе. Task ID, Goal, Scope, Acceptance Criteria и пять исходных source/test/design anchors сохрани. Не меняй смысл контракта.
- В `STATE.md` запиши фактические HEAD/branch, коммит Phase 1B, fast-forward `main` и состояние следующего Goal: handoff готов, реализация не начата. Явно сохрани запрет автоматического перехода к реализации в этом задании.
- В `docs/PHASE-1B-COMMIT.md` отметь выполненный коммит, его SHA и фактическую проверку состава. Описание временного патча оставь как историческое evidence.
- В этом документе добавь короткий результат перехода с SHA и итоговой веткой, чтобы повторное чтение не побуждало исполнять его заново.

`ARCHITECTURE.md` и `docs/READ-PATH-DECISIONS.md` должны сохранить уже подготовленный дизайн. Исторический assertion handoff должен остаться byte-identical. Отчёт независимого аудита не переписывай как новый аудит. Не создавай второй коммит с планом: документы следующего Goal остаются рабочими изменениями новой ветки.

## 6. Проверка и точка остановки

Проверь source/test/design anchors, сохранность исторического handoff, ссылок в документах, `git diff --check`, пустой index после коммита, итоговый status и ссылки веток. Не называй дерево чистым: плановые документы и локальные артефакты намеренно остаются незакоммиченными. Новых `src/policy/decisions.ts` и `test/decisions.test.ts` быть не должно.

Закончи отчётом: SHA коммита, результат fast-forward, активная ветка, обновлённый baseline, какие рабочие изменения сохранены, выполненные проверки. Подтверди отсутствие push и реализации следующего Goal. На этом остановись.
